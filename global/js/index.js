/**
 * Scrolls the page to the element that matches the given selector.
 *
 * @param {string} elementSelector - A CSS selector that matches the element to scroll to.
 * @param {number} [instance=0] - The instance number of the element to scroll to. If there are multiple elements that match the selector, this parameter can be used to specify which instance to scroll to.
 */
function scrollToElement(elementSelector, instance = 0) {
    
}